import 'package:flutter/material.dart';

class HadesTab extends StatelessWidget {
  const HadesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
